# 复制成 .env 后按飞牛上的实际路径改（.env 不要提交进仓库）

# 应用根目录（含 server/ scripts/ plugins/ 的那一层，也就是解包出来的 valimart-gateway/）。
# 必须写绝对路径：散文件形式和 tar 解包形式的目录层级不同，用相对路径容易挂错一级。
APP_DIR=/vol1/1000/harness/valimart-gateway

# 数据目录：飞牛上的 SSD 卷。gateway.sqlite + 公司盘 drive/ 都落这里。
# 千万别放 SD 卡（频繁小写伤卡）；用 /vol1/... 这种存储卷路径。
GATEWAY_DATA=/vol1/1000/harness/data

# 对外端口（宿主机端口，容器内固定 8790）
GATEWAY_PORT=8790

# 上游模型密钥（可选，只放服务端）。也可以留空，改用管理页「设置 → 同事 → 模型通道」接入。
DEEPSEEK_API_KEY=
ANYSEARCH_API_KEY=
