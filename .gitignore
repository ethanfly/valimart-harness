# 依赖
node_modules/

# 网关运行数据（账号 / 登录会话 / 令牌 / 任务 / 账本 / 通道凭据 / 公司盘）——含凭据，绝不入库；首次启动自动按 config.json 播种
server/data/
server/config.local.json

# 树莓派代理订阅清单——含真实订阅 token，绝不入库（模板见 subscriptions.example.yaml）
deploy/raspberry-pi/subscriptions.yaml

# 客户端浏览器端 bundle（launch.mjs / npm run build 自动生成）
plugins/desk-ui/lib/client.js

# Playwright
/test-results/
/playwright-report/
/blob-report/

# 杂项
*.log
.DS_Store
Thumbs.db
__pycache__/
*.pyc
jar.txt

# 本地探针 / 验收落盘（不入库）
/tmp-*.mjs
/tmp-*.cjs
/docs/evidence/mixed/
/e2e/artifacts/
/scripts/tmp-*.mjs
/scripts/evidence-*.mjs

# 安装包构建产物
/build/
dist/
desktop/dist/
desktop/build/verify-app/
desktop/build/titlebar-verify.*
desktop/build/argv.json
